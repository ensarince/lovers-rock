# 1. Use a lightweight Linux base
FROM alpine:latest

# 2. Define the PocketBase version
ARG PB_VERSION=0.26.5

# 3. Install necessary tools to download and unzip
RUN apk add --no-cache \
    ca-certificates \
    unzip \
    wget \
    bash

# 4. Download the Linux version of PocketBase (NOT the .exe)
RUN wget https://github.com/pocketbase/pocketbase/releases/download/v${PB_VERSION}/pocketbase_${PB_VERSION}_linux_amd64.zip \
    && unzip pocketbase_${PB_VERSION}_linux_amd64.zip -d /pb/

# 5. Copy your local migrations and hooks into the container
# Hooks are copied to /pb/pb_hooks (separate from the data volume).
# start.sh copies them into pb_data/pb_hooks at runtime so PocketBase finds them.
COPY ./pb_migrations /pb/pb_migrations
COPY ./pb_hooks /pb/pb_hooks
COPY ./start.sh /pb/start.sh
RUN chmod +x /pb/start.sh

# 6. Expose the port Railway uses
EXPOSE 8080

# 7. Start via script that syncs hooks then launches PocketBase
CMD ["/pb/start.sh"]
